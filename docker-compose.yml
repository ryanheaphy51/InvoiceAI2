version: "3.9"
services:
  invoiceai:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - server/.env
    volumes:
      - ./server/data:/app/server/data
    restart: unless-stopped
